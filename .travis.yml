language: node_js
node_js:
  - 12

services:
  - postgresql

before_script:
  - psql -c 'create database taxi_trips;' -U postgres
  - psql -a -d taxi_trips -f sql/tables.SQL -U postgres
  - psql -a -d taci_trips -f sql/data.SQL -U postgres